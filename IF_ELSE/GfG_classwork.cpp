#include <iostream>
#include <cmath>
using namespace std;

//CHECKING FOR EVEN ODD NUMBER
/* int main() {
  int n;

  cout << "Enter an integer: ";
  cin >> n;

  if ( n % 2 == 0)
    cout << n << " is even.";
  else
    cout << n << " is odd.";

  return 0;
} */

// checking EVEN ODD & ZERO with SIGN
/* int main()
{
    int n;
    cout<<"Enter the number:";
    cin>>n;

    if(n%2==0 && n>0)
    cout<<"Positive Even";

    else if(n%2==0 && n<0)
    cout<<"Negative Even";

    else if(n%2==1 && n>0)
    cout<<"Positive Odd";

    else if(n%2==1 && n<0)
    cout<<"Negative Odd";


    else
    cout<<"Zero";
     
    return 0;
} */

//CHECKING + - & 0
/* int main()
{
    int n;
    cout<<"Enter the number:";
    cin>>n; 

    if(n>0)
    cout<<"Positive Number\n";

    else if(n<0)
    cout<<"Negative Number\n";

    else
    cout<<"Zero\n";
    
    return 0;
} */























